name: CI:Build
on:
  push:
    branches:
      - master
      - stable
jobs:
  build:
    name: ci-build
    runs-on: ubuntu-latest
    steps:
      - name: make
        id: make
      - name: make (compile)
        run: make
